<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Фон маркера -->
    <solid android:color="@color/seekbar_thumb_normal" />

    <!-- Обводка маркера -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" />
</shape>